import styled from 'styled-components';
import { variables } from '../shared/GlobalStyles';

export const Roadmap = () => {
	return <Container className='section'>
        <span className="title">Walk with us</span>
    </Container>;
};

const Container = styled.section``;
